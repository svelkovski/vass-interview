# How to start it?

## Open backend project folder with Intellij and run the BackendApplication class
## Open the frontend project folder with vscode and run npm install then npm run dev
## App should start on http://localhost:5173, if you run it on a different port update the cors config in the backend project under WebConfig class
